import React from 'react'

/**
 * Represents a account dashboard.
 * 
 * @returns {*} Returns a dashboard component.
 */
function account() {
  return (
    <div>Dashboard</div>
  )
}

export default account
